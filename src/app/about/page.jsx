import React from 'react';
import AboutPage from '../[component]/About/AboutPage';

const page = () => {
    return (
        <section>
            <AboutPage/>
        </section>
    );
};

export default page;
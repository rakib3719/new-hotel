
import React from 'react';
import dynamic from 'next/dynamic';
import Login from '../[component]/LogIn/Login';


const page = () => {
    return (
        <div>
            <Login/>
        </div>
    );
};

export default page;